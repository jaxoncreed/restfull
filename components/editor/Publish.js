var React = require('react');

var Publish = React.createClass({
    render: function() {
        return (
            <div>
                <p>Publish</p>
            </div>
        );
    }
});

module.exports = Publish;